---
name: Publish
on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'

jobs:
  publish:
    runs-on: ubuntu-latest
    environment: release
    if: github.repository_owner == 'ATIX-AG'

    permissions:
      id-token: write

    steps:
      - uses: voxpupuli/ruby-release@v0
